namespace MiniNetflix.Identity.DTOs
{
    /// <summary>
    /// DTO подписки пользователя (ответ от сервиса Subscription).
    /// </summary>
    public class SubscriptionDto
    {
        // Поля намеренно не добавлены.
        // Студенты самостоятельно добавят нужные свойства (тип подписки, даты, статус и т.д.).
    }
}

